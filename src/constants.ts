export const DEFAULT_OPTION_ITEM_HEIGHT = 40;
export const DEFAULT_VISIBLE_COUNT = 5;
export const DEFAULT_PERSPECTIVE = 2000;
export const DEFAULT_ENABLE_ITEM_PRESS = true;
export const SCROLL_OFFSET_THRESHOLD = 0.5;
export const SCROLL_ANIMATION_DURATION = 100; // ms
export const MAX_TICK_LATENCY_MS = 100; // ms
export const MIN_TICK_INTERVAL_MS = 32; // ms

/** Below this speed (px/ms) a mouse release stops in place with no momentum scroll. Web only. */
export const MIN_MOMENTUM_VELOCITY = 0.05;
/** Per-millisecond momentum decay — the feel of `decelerationRate="fast"` on native. Web only. */
export const DECELERATION_RATE = 0.99;
